services:
  db:
    image: postgres:16-alpine
    container_name: agnos_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-his}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-his}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      target: dev
    container_name: agnos_api
    depends_on:
      db:
        condition: service_healthy
    environment:
      APP_PORT: ${APP_PORT:-8080}
      APP_SECRET: ${APP_SECRET:-CHANGE_ME_APP_SECRET}
      DB_URL: postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-his}?sslmode=disable
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - ./:/app
      - go_mod_cache:/go/pkg/mod
    command: ["go", "run", "./cmd/main.go"]

volumes:
  postgres_data:
  go_mod_cache: