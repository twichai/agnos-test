info:
  name: patient-search
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/patient/search/:id"
  params:
    - name: id
      value: JP765432
      type: path
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
