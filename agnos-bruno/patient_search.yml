info:
  name: patient-search
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/patient/search/1100000000001"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
