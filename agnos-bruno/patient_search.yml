info:
  name: patient-search
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/patient/search/0861111111"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
