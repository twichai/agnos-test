info:
  name: staff-login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/staff/login
  body:
    type: json
    data: |-
      {
        "username": "staff_a",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
