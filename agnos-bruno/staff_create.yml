info:
  name: staff-create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/staff/create"
  body:
    type: json
    data: |-
      {
        "username": "staff_a",
        "password": "1234",
        "hospital_id": "{{hospitalID}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
